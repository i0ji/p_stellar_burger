import authSlice from "slices/authSlice.ts";
import constructorSlice from "slices/constructorSlice.ts";
import currentIngredientSlice from "slices/currentIngredientSlice.ts";
import orderSlice from "slices/orderSlice.ts";
import orderFeedSlice from "slices/orderFeedSlice.ts";
import ingredientsSlice from "slices/ingredientsSlice.ts";

export {
    authSlice,
    constructorSlice,
    currentIngredientSlice,
    orderSlice,
    orderFeedSlice,
    ingredientsSlice
}