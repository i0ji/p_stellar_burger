// import {IIngredient} from "declarations/interfaces";
//
// export const ingredientCount = (ingredientsArray: Array<IIngredient>) => {
//     ingredientsArray.reduce((count: number, ingredient: IIngredient) => {
//         return ingredient.name === name ? count + 1 : count;
//     }, 0)
// }